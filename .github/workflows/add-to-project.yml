name: automatically add to project board
on:
  issues:
    types:
      - opened
  pull_request:
    types:
      - opened

jobs:
  add_to_project:
    uses: gliff-ai/.github/.github/workflows/add-to-project.yml@v1
    secrets:
        ROBOT_PAT: ${{ secrets.ROBOT_PAT }}
