import * as React from "react";
import * as ReactDOM from "react-dom";

import {BaseCanvas} from "./baseCanvas"


ReactDOM.render(
  <BaseCanvas name="cccc" />,
  document.getElementById("react-container")
);
